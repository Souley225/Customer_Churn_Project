# Variables d'environnement pour déploiement
# Copier ce fichier en .env et remplir les valeurs

# ============================================================================
# MODE DE CHARGEMENT DES ARTEFACTS
# ============================================================================

# Utiliser les artefacts locaux (data/processed/*.joblib) sans MLflow
# Mettre à "true" pour déploiement sur Render ou autre plateforme sans MLflow
USE_LOCAL_ARTIFACTS=true

# ============================================================================
# CONFIGURATION MLFLOW (seulement si USE_LOCAL_ARTIFACTS=false)
# ============================================================================

# URI du serveur de tracking MLflow
# Si USE_LOCAL_ARTIFACTS=false et cette variable est vide, utilisera le fallback local
# MLFLOW_TRACKING_URI=

# URI du modèle MLflow
# Par défaut: models:/telco-churn-classifier/Production
# Alternative: pointer vers un run spécifique runs:/<run_id>/model
# MODEL_URI=models:/telco-churn-classifier/Production

# ============================================================================
# AUTRES
# ============================================================================

# Port pour l'API (défini automatiquement par Render)
# PORT=8000

# Configuration Kaggle (pour téléchargement de données en développement)
# KAGGLE_USERNAME=
# KAGGLE_KEY=
